# Kimi Code CLI configuration template
# Generated by gum installer. Customize as needed.
# Docs: https://moonshotai.github.io/kimi-cli/en/configuration/config-files.html

# ---------------------------------------------------------------------------
# Default behavior
# ---------------------------------------------------------------------------

# Model to use when no --model flag is passed (must match a key in [models])
default_model = "qwen3-coder-next"

# Enable extended thinking by default (set true for complex reasoning tasks)
default_thinking = false

# YOLO mode: auto-approve all file edits and shell commands without prompting.
# Leave false for safety; toggle per-session with /yolo when needed.
default_yolo = false

# ---------------------------------------------------------------------------
# Providers
# ---------------------------------------------------------------------------
# Valid types: kimi, openai_legacy, openai_responses, anthropic, google_genai,
#              gemini, vertexai
# API keys can also be set via environment variables (preferred for security).

# --- Local llama-server (OpenAI-compatible) ---
# Matches the provider defined in opencode.json so you can use the same
# local model across both OpenCode and Kimi Code. Works fully offline.

[providers.llama-local]
type = "openai_legacy"
base_url = "http://10.0.0.194:8080/v1"
api_key = "no-key-needed"

# --- Optional: Ollama (local) ---
# Another local option if you run Ollama instead of llama-server.

# [providers.ollama]
# type = "openai_legacy"
# base_url = "http://localhost:11434/v1"
# api_key = "ollama"

# --- Anthropic ---
# SECURITY: Prefer setting ANTHROPIC_API_KEY env var instead of hardcoding.

# [providers.anthropic]
# type = "anthropic"
# base_url = "https://api.anthropic.com"
# api_key = "sk-ant-..."

# --- OpenAI ---
# SECURITY: Prefer setting OPENAI_API_KEY env var instead of hardcoding.

# [providers.openai]
# type = "openai_responses"
# base_url = "https://api.openai.com/v1"
# api_key = "sk-..."

# --- Google Generative AI ---
# SECURITY: Prefer setting GOOGLE_GENERATIVE_AI_API_KEY env var instead of hardcoding.

# [providers.google]
# type = "google_genai"
# api_key = "AIza..."

# --- Kimi / Moonshot ---
# SECURITY: Prefer setting MOONSHOT_API_KEY env var instead of hardcoding.

# [providers.kimi]
# type = "kimi"
# base_url = "https://api.kimi.com/coding/v1"
# api_key = "sk-..."

# --- Mistral ---
# SECURITY: Prefer setting MISTRAL_API_KEY env var instead of hardcoding.

# [providers.mistral]
# type = "openai_legacy"
# base_url = "https://api.mistral.ai/v1"
# api_key = "..."

# --- Groq ---
# SECURITY: Prefer setting GROQ_API_KEY env var instead of hardcoding.

# [providers.groq]
# type = "openai_legacy"
# base_url = "https://api.groq.com/openai/v1"
# api_key = "gsk_..."

# --- Vertex AI ---
# Uses Google Cloud credentials (ADC). No api_key needed if gcloud is configured.

# [providers.vertexai]
# type = "vertexai"
# base_url = "https://us-central1-aiplatform.googleapis.com/v1"

# ---------------------------------------------------------------------------
# Models
# ---------------------------------------------------------------------------
# Each model references a provider and specifies the API model identifier.
# max_context_size is in tokens.
# capabilities: "thinking" (extended thinking), "image_in" (vision input)

# --- Active: local models ---

# Local Qwen model via llama-server (matches opencode.json llama-local/qwen3-coder-next)
[models.qwen3-coder-next]
provider = "llama-local"
model = "qwen3-coder-next"
max_context_size = 32768

# --- Optional: Ollama local models ---

# [models.qwen3-coder]
# provider = "ollama"
# model = "qwen3-coder"
# max_context_size = 32768

# [models.deepseek-coder-v2]
# provider = "ollama"
# model = "deepseek-coder-v2"
# max_context_size = 131072

# --- Optional: Anthropic models ---

# [models.claude-sonnet]
# provider = "anthropic"
# model = "claude-sonnet-4-5"
# max_context_size = 200000
# capabilities = ["thinking", "image_in"]

# [models.claude-opus]
# provider = "anthropic"
# model = "claude-opus-4"
# max_context_size = 200000
# capabilities = ["thinking", "image_in"]

# [models.claude-haiku]
# provider = "anthropic"
# model = "claude-haiku-4-5"
# max_context_size = 200000
# capabilities = ["image_in"]

# --- Optional: OpenAI models ---

# [models.gpt-4o]
# provider = "openai"
# model = "gpt-4o"
# max_context_size = 128000
# capabilities = ["image_in"]

# [models.gpt-4-1]
# provider = "openai"
# model = "gpt-4.1"
# max_context_size = 1047576
# capabilities = ["image_in"]

# [models.o3]
# provider = "openai"
# model = "o3"
# max_context_size = 200000
# capabilities = ["thinking", "image_in"]

# --- Optional: Google models ---

# [models.gemini-2-5-pro]
# provider = "google"
# model = "gemini-2.5-pro"
# max_context_size = 1048576
# capabilities = ["thinking", "image_in"]

# [models.gemini-2-5-flash]
# provider = "google"
# model = "gemini-2.5-flash"
# max_context_size = 1048576
# capabilities = ["thinking", "image_in"]

# --- Optional: Kimi / Moonshot models ---

# [models.moonshot-v1-128k]
# provider = "kimi"
# model = "moonshot-v1-128k"
# max_context_size = 128000

# [models.moonshot-v1-32k]
# provider = "kimi"
# model = "moonshot-v1-32k"
# max_context_size = 32000

# [models.moonshot-v1-8k]
# provider = "kimi"
# model = "moonshot-v1-8k"
# max_context_size = 8000

# --- Optional: Mistral models ---

# [models.mistral-large]
# provider = "mistral"
# model = "mistral-large-latest"
# max_context_size = 128000

# [models.codestral]
# provider = "mistral"
# model = "codestral-latest"
# max_context_size = 256000

# --- Optional: Groq models ---

# [models.llama-3-3-70b]
# provider = "groq"
# model = "llama-3.3-70b-versatile"
# max_context_size = 128000

# [models.llama-3-1-8b]
# provider = "groq"
# model = "llama-3.1-8b-instant"
# max_context_size = 131072

# --- Optional: Vertex AI models ---

# [models.gemini-2-5-pro-vertex]
# provider = "vertexai"
# model = "gemini-2.5-pro"
# max_context_size = 1048576
# capabilities = ["thinking", "image_in"]

# ---------------------------------------------------------------------------
# Loop control
# ---------------------------------------------------------------------------
# Governs the agentic execution loop. Defaults are sensible for most workflows.

[loop_control]
max_steps_per_turn = 100      # Max tool-call steps per user turn
max_retries_per_step = 3      # Retries on transient errors per step
max_ralph_iterations = 0      # Self-reflection iterations (0 = disabled)
reserved_context_size = 50000 # Tokens reserved for system prompt + response

# ---------------------------------------------------------------------------
# MCP (Model Context Protocol)
# ---------------------------------------------------------------------------
# MCP servers are configured in ~/.kimi/mcp.json (generated by gum installer).
# This section controls MCP client behavior only.

[mcp.client]
tool_call_timeout_ms = 60000

# ---------------------------------------------------------------------------
# Services (optional)
# ---------------------------------------------------------------------------
# Built-in Moonshot services for web search and URL fetching.
# Uncomment if you have Moonshot API access.

# [services.moonshot_search]
# base_url = "https://api.moonshot.cn"
# api_key = "sk-..."

# [services.moonshot_fetch]
# base_url = "https://api.moonshot.cn"
# api_key = "sk-..."
